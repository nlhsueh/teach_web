
// 易
::Question 1::在 Django 專案中，哪個指令用於啟動開發伺服器？{
    =python manage.py runserver#正確答案：`runserver` 用於啟動 Django 內建的開發伺服器。
    ~python manage.py startapp
    ~python manage.py migrate
    ~python manage.py createsuperuser
}

// 易
::Question 2::Django 遵循哪種設計模式？{
    =MTV (Model-Template-View)#正確答案：Django 遵循 Model-Template-View 模式，與傳統的 MVC (Model-View-Controller) 有所不同。
    ~MVC (Model-View-Controller)
    ~MVVM (Model-View-ViewModel)
    ~MVP (Model-View-Presenter)
}

// 易
::Question 3::在 Django 中，用來處理資料庫操作的層是？{
    =Model#正確答案：Model 負責定義資料結構和資料庫互動。
    ~View
    ~Template
    ~URLconf
}

// 易
::Question 4::哪個檔案負責 Django 專案的 URL 路由設定？{
    =urls.py#正確答案：`urls.py` 定義了 URL 模式與視圖函數的映射關係。
    ~views.py
    ~models.py
    ~settings.py
}

// 易
::Question 5::在 Django 模版中，要顯示變數 `my_variable` 的正確語法是？{
    ={{ my_variable }}#正確答案：雙大括號 `{{ }}` 用於顯示變數。
    ~{% my_variable %}
    ~< my_variable >
    ~`my_variable`
}

// 中
::Question 7::要建立一個新的 Django 應用程式，應該使用哪個指令？{
    =python manage.py startapp myapp#正確答案：`startapp` 用於建立新的應用程式。
    ~python manage.py createapp myapp
    ~django-admin startapp myapp
    ~python manage.py makeapp myapp
}

// 中
::Question 8::在 Django 的 Model 中，要定義一個字串欄位，應該使用哪個欄位類型？{
    =models.CharField#正確答案：`CharField` 用於儲存短字串。
    ~models.TextField
    ~models.IntegerField
    ~models.BooleanField
}

// 中
::Question 9::哪個方法用於在 Django 的 View 中渲染一個模版並傳遞上下文資料？{
    =render()#正確答案：`render()` 函數用於渲染模版並將資料傳遞給它。
    ~HttpResponse()
    ~redirect()
    ~JsonResponse()
}

// 中
::Question 10::在 Django 資料庫遷移(migrations)中，哪個指令會將模型的更改應用到資料庫？{
    =python manage.py migrate#正確答案：`migrate` 會執行之前創建的遷移，將模型更改應用到資料庫。
    ~python manage.py makemigrations
    ~python manage.py syncdb
    ~python manage.py dbshell
}

// 中
::Question 11::在 Django ORM 中，要取得一個模型的所有物件，應該使用哪個語法？{
    =MyModel.objects.all()#正確答案：`objects.all()` 用於獲取指定模型的所有實例。
    ~MyModel.objects.get()
    ~MyModel.objects.filter()
    ~MyModel.objects.create()
}

// 中
::Question 12::在 Django 的 Form 處理中，當表單提交後，哪個方法用於檢查表單資料的有效性？{
    =form.is_valid()#正確答案：`is_valid()` 方法會驗證表單提交的資料。
    ~form.save()
    ~form.clean()
    ~form.as_p()
}


// 難
::Question 14::當您在 Django 的 Model 中添加一個新的欄位後，要更新資料庫結構，通常需要執行哪兩個步驟？{
    =首先 `makemigrations`，然後 `migrate`#正確答案：`makemigrations` 創建遷移文件，`migrate` 應用這些遷移文件到資料庫。
    ~首先 `migrate`，然後 `makemigrations`
    ~只需 `makemigrations`
    ~只需 `migrate`
}


// 中
::Question 21::在 Django 的 View 函數中，如何將資料從 View 傳遞到 Template？{
    =將資料作為字典傳遞給 `render()` 函數的第三個參數#正確答案：`render()` 函數的第三個參數是一個字典，其中的鍵值對會被傳遞到模版中作為上下文變數。
    ~直接在 Template 中定義變數
    ~使用全域變數
    ~透過 HTTP 請求頭傳遞
}

// 中
::Question 22::在 Django 模版中，要繼承一個基礎模版並覆寫其中的區塊 (block)，應該使用哪個標籤？{
    = {% extends %} 和 {% block %}#正確答案：`{% extends %}` 用於繼承，`{% block %}` 用於定義可覆寫的區塊。
    ~ {% include %} 和 {% load %}
    ~ {% static %} 和 {% url %}
    ~ {% csrf_token %} 和 {% if %}
}

// 中
::Question 23::在 Django 中，哪個內建模組主要負責使用者認證（Authentication）？{
    =django.contrib.auth#正確答案：`django.contrib.auth` 模組提供了使用者認證、權限管理等功能。
    ~django.contrib.admin
    ~django.contrib.sessions
    ~django.contrib.messages
}

// 中
::Question 24::要讓某個 View 函數只允許已登入的使用者訪問，可以使用哪個裝飾器 (decorator)？{
    = @login_required#正確答案：`@login_required` 裝飾器會檢查使用者是否已登入，如果未登入會重定向到登入頁面。
    ~ @permission_required
    ~ @staff_member_required
    ~ @user_passes_test
}

// 中
::Question 25::在 Django Model 中，要確保某個欄位的值是唯一的，應該設定哪個參數？{
    = unique=True#正確答案：`unique=True` 會在資料庫層面強制欄位的唯一性。
    ~ primary_key=True
    ~ default=None
    ~ null=True
}

// 中
::Question 26::在 Django 的 View 函數中，如何判斷目前的 HTTP 請求方法是 GET 還是 POST？{
    = request.method == 'POST' 或 request.method == 'GET'#正確答案：`request.method` 屬性會返回請求的 HTTP 方法（例如 'GET', 'POST'）。
    ~ request.is_post() 或 request.is_get()
    ~ request.type == 'POST' 或 request.type == 'GET'
    ~ request.form_data
}